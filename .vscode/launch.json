{
  "configurations": [
    {
      "name": "Run and debug C Program",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": ["tests_acte3/acte3_amphibians.test_dragon"],
      "cwd": "${fileDirname}",
      "preLaunchTask": "makelldb: make"
    }
  ]
}